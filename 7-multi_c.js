// Get the number of times to print from the command line argument
const x = parseInt(process.argv[2]);

// Loop x times and print "C is fun"
for (let i = 0; i < x; i++) {
  console.log("C is fun");
}
